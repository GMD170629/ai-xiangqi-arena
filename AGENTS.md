# AGENTS.md

本文件是 Codex 与其他编码代理在本仓库中的最高优先级项目约束之一。

## 1. 产品不变量

1. 人类玩家永远不得直接修改棋盘状态或移动棋子。
2. 所有实际棋步必须来自配置的 AI Player。
3. 禁止引入或调用 Pikafish、Stockfish 派生象棋引擎或其他“最佳着”决策引擎。
4. Xiangqi Rules 模块只负责规则，不得参与决策。
5. 禁止在规则层实现局面评分、最佳着搜索、推荐着法、Alpha-Beta、MCTS 或其他棋力算法。
6. 玩家对 AI 的影响必须通过自然语言 Commander Message 进入 AI 上下文。
7. AI 可以接受、部分接受或拒绝玩家建议。
8. AI 人格通过 Prompt/Profile 影响 AI，不通过游戏代码伪造固定走棋偏好。

## 2. 模块边界

### Game Runtime

负责：
- 对局生命周期
- 回合推进
- 暂停/继续
- 对局状态
- 失败恢复

不得：
- 计算哪一步更好
- 自动替 AI 选择棋步

### Xiangqi Rules

负责：
- 棋盘表示
- 走法解析
- 合法性校验
- 将军/将死
- 和棋与终局判断
- 合法着列表（仅作为规则信息）

不得：
- 局面评分
- 棋步排序
- 最佳着推荐

### AI Runtime

负责：
- 构造 AI 上下文
- 调用 Provider
- 解析结构化返回
- 处理超时、非法着、格式错误与重试

### AI Provider

首版优先：
- OpenAI Compatible API
- Localhost/LAN/Cloud 均允许

业务层不得散落 provider-specific 条件判断。

### UI

负责：
- 棋盘展示
- AI 状态展示
- Commander 输入
- 人格配置
- 对局历史
- 动效与趣味提示

UI 中不得存在可直接控制棋子落点的人类交互。

## 3. AI 输出原则

首版要求 AI 返回最少：

```json
{
  "move": "...",
  "message": "..."
}
```

`message` 是面向玩家的简短解释，不要求、也不依赖模型暴露完整隐藏推理链。

## 4. 开发原则

- TypeScript 严格模式。
- 核心规则必须有单元测试。
- AI 网络调用与规则逻辑必须可独立测试。
- Prompt 必须版本化。
- 所有对局事件必须可记录和回放。
- API Key 不得写入日志、仓库或对局记录。
- 动画不得阻塞对局状态机。
- 趣味文案不得影响规则和棋局结果。

## 5. 首版范围控制

除非明确修改 MVP 文档，否则不要主动加入：
- 排位
- 好友系统
- 商城
- AI 养成数值
- 指挥点
- 技能/卡牌
- 象棋教学
- 引擎分析
- 中心化模型托管
